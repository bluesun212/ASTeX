\usepackage{xcolor}

% this line should be left unchanged
\newcommand{\tcgreen}[1]{\textcolor{green}{#1}}

%\newcommand{\tcred}[1]{\textcolor{red}{#1}}
%\newcommand{\tcblue}[1]{\textcolor{blue}{#1}}

\begin{document}
\begin{testenv}{\LaTeX}
This is perfectly valid \textbf{\LaTeX{} code with \tcred{some \textit{nested}
macro} invocations} for coloring and styling
% to make it more complicated it also contains \tcblue{some \tcred{nested} macros} in comments
\end{testenv}

\code
\end{document}
